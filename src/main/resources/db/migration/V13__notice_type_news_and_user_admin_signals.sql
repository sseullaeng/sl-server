-- =============================================================================
-- V13 — Notice 'type' ENUM 에 '새소식' 추가
--
-- 프론트 NoticeWritePage 의 "새소식" 옵션 활성화. 기존 데이터는 영향 없음 (DEFAULT 유지).
-- =============================================================================

ALTER TABLE notices
    MODIFY COLUMN type ENUM('공지','이벤트','새소식') NOT NULL DEFAULT '공지';
